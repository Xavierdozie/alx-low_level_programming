#include "main.h"

/**
 * main - Tests function prints if integer is positive or negative
 * Returns: 0
 */

int main(void)
{
	void positive_or_negative(int i);

	i = 0;
	positive_or_negative(i);

	return (0);
}
