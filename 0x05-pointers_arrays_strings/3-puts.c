#include "main.h"
#include <unistd.h>
/**
 * _puts - prints a string,to stdout
 * @str: value to be evaluated
 * Return: not.
 */
void _puts(char *str)
{
	int i = 0;

	 while (*(str + l) != '\0')
	 {
		 _putchar(str[l]);
		 i++;
	 }
	 _putchar('\n');
}
